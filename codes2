#!/bin/bash

PDIR=`dirname "$0"`
cd ${PDIR}

if command -v python3 >/dev/null 2>&1; then
    PYTHON=python3
elif command -v python >/dev/null 2>&1; then
    PYTHON=python
else
    echo "Could not find Python interpreter (python3/python)." >&2
    exit 1
fi

exec "${PYTHON}" -O codes2.py "$@"
